get:
  tags:
    - Contacts
  summary: Отримати контакт за ID
  operationId: getContactById
  description: >
    Повертає інформацію про контакт за його унікальним ID.
  security:
    - bearerAuth: []
  parameters:
    - name: contactId
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      description: Контакт знайдено
    '404':
      description: Контакт не знайдено
    '401':
      description: Неавторизований
